<% if user_signed_in? %>
    Logged as <%= current_user.login %> | <%= link_to 'Disconnect', destroy_user_session_path, :method => :delete  %>
<% else %>
    <%= link_to 'Sign in...', new_user_session_path %>
<% end %>

<hr />

<% if user_signed_in? %>
  <%=  link_to 'Albums', albums_path %>
<% else %>
  Derpy derp o_O
<% end %>

